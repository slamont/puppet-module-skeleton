# == Class <%= metadata.name %>::params
# This class is meant to be called from <%= metadata.name %>
# It set variable according to platform
class <%= metadata.name %>::params {
  $pkgname = '<%= metadata.name %>'
  $conffile = '/etc/<%= metadata.name %>.conf'
  $example_file = '/etc/<%= metadata.name %>_example.conf'
  $example_param = 'Example Value',
  $example_hash = {
    'ex_key1' => 'ex_value1',
    'ex_key2' => 'ex_value2',
  }
  $service = $::osfamily ? {
    'Debian' => '<%= metadata.name %>',
    'RedHat' => '<%= metadata.name %>d',
    default  => fail('unsupported platform')
  }
}
